# Agent Progress

## 2026-07-12

- Normalized existing local `output/` workbook filenames using each XLSX file's embedded creation timestamp as the analysis date. The historical `20260703_172748` file was actually created on 2026-07-05 and was renamed accordingly; the `171754` and `180950` pairs were created on 2026-07-12. The superseded `190712` originals were retained with an `_original` suffix because corrected `20260712_190712` files already exist. No output file was deleted or overwritten.
- User identified that filenames should show the analysis/run date, not the latest exchange close date. Updated future output naming to use one local `datetime.now()` snapshot for `YYYYMMDD_HHMMSS`; `close_date` remains inside the workbook as the authoritative market-data date. Updated README, guide text, and dictionary wording. Preserved all existing local files and created correctly named copies of the completed production pair as `output/tw_valuation_gap_20260712_190712.xlsx` and `_lite.xlsx`.
- Monitored the user's real production scan rather than guessing from existing filenames. Confirmed PID 59372 was `src/tw_target_scan.py` with the conservative full-scan Cnyes settings, started at 18:11:44 and completed at 19:07. New outputs are `output/tw_valuation_gap_20260709_190712.xlsx` and `_lite.xlsx`. Both have 1,980 rows (1,089 listed + 891 OTC), Cnyes status 1,973 ok / 7 no-target, 1,926 nonblank gross margins, no blank source columns, zero blank cell nodes, and the 53-row guide including full/lite equivalence. Excel opened full in 934 ms and lite in 393 ms without repair. All older local output files were preserved.
- Corrected the output-reference selection after the user clarified that `171754` was not the intended latest deliverable. Regenerated a fresh 500-stock no-Cnyes pair, `output/tw_valuation_gap_20260709_180950_no_cnyes.xlsx` and `_lite.xlsx`, without deleting any existing local files. Both contain exactly 250 listed + 250 OTC rows, 496 nonblank gross margins, no blank source columns, zero blank cell nodes, and the new full/lite guide rows. README now identifies this 500-stock pair and explicitly states that Cnyes target fields are blank because it was generated with `--skip-cnyes`.
- User requested replacing the tracked `output/` sample with the latest run and confirming lite documentation. Validated `output/tw_valuation_gap_20260709_171754.xlsx` and `_lite.xlsx`: 1,980 rows (1,089 listed + 891 OTC), Cnyes 1,971 ok and 9 no-target, 1,926 nonblank gross margins, no blank source columns, and zero blank cell nodes. Full has 7 sheets/63 stock columns; lite has 6 sheets/43 stock columns and omits 20 detail fields plus the stale/low-confidence sheet while using the same crawl and calculations. Added explicit full/lite/data-equivalence explanations to README and workbook guide generation.
- Cleaned 95 historical local artifacts from `output/`, retaining only `.gitkeep` and the latest `20260709_171754` full/lite pair. Added the same three full/lite explanation rows directly to both retained workbooks. Excel opened the retained full workbook in 613 ms and lite in 349 ms without repair; both have 1,981 used rows including the header and their expected 63/43 columns. The old tracked `20260703_172748.xlsx` is removed; the new pair remains ignored until intentionally force-added during the next Git sync.
- User requested a 500-stock review run. The first attempt included Cnyes with conservative pacing, but the parent command hit its 10-minute tool limit; the orphaned Python process continued for about 52 minutes and then exited without writing a workbook, so it was not reported as complete. Produced a verified 500-stock fundamentals-only review instead: `output/tw_valuation_gap_20260709_160301_no_cnyes.xlsx` and `_lite.xlsx`, exactly 250 listed + 250 OTC rows. OTC gross margin coverage was 250/250 and listed coverage 246/250; all valuation/monthly-revenue/financial source cells were populated and XLSX blank-cell nodes were zero. Excel COM opened full in 291 ms and lite in 154 ms without repair, each with 501 used rows including the header.
- Rechecked the current TPEx Swagger after the user questioned all-blank OTC gross margins and found the stable official endpoint `mopsfin_187ap17_O` (not `mopsfin_t187ap17_O`). It currently exposes company code, year/quarter, gross margin, operating margin, pretax margin, and net margin. Integrated it into OTC fundamentals, made official TPEx ratios primary with calculated operating/net margins only as field-level fallbacks, removed the obsolete all-OTC missing-margin note, and added gross margin to lite/stale review columns.
- Widened `財報資料來源` to 50 after adding the second TPEx endpoint so the combined source text stays inside the cell.
- Completed the requested live 50-stock rerun with Cnyes after the TPEx margin integration: `output/tw_valuation_gap_20260709_144122.xlsx` and `_lite.xlsx`, 25 listed + 25 OTC, Cnyes 50/50 ok. Gross margin was populated for all 25 listed and all 25 OTC sample rows; OTC 1240 was 12.79% in Excel and exactly matched the TPEx official `毛利率` value 12.79. OTC row-level margin warning notes are now blank. The lite workbook now includes gross margin. Both workbooks have zero blank cell nodes; Excel COM opened full in 149 ms and lite in 82 ms without repair, with valuation-source width 39.22 and financial-source width 49.22 displaying the full combined TPEx source.
- Ran a fresh full-market fundamentals audit after the user asked whether other blanks came from similarly missed endpoints: `output/tw_valuation_gap_20260709_144927_no_cnyes.xlsx` and `_lite.xlsx`, 1,980 rows. No second clearly missed like-for-like aggregate endpoint was found. Remaining blanks split into official/non-applicable cases: OTC gross margin missing 7/891, all securities/futures companies excluded from the TPEx margin aggregate; listed gross margin missing 47/1,089, comprising 32 financial firms, 10 TDRs, and 5 other companies also absent from the official general-industry income endpoint. PER blanks are mostly official invalid/blank ratios (254 OTC and 247 listed field-level blanks); monthly-revenue rows are absent from the current official OpenAPI batch for 81 OTC and 147 listed stocks; EPS is complete for OTC and missing for 11 listed stocks, mainly TDRs. The 9 rows missing all three fundamentals groups are TDRs. TWSE/TPEx Swagger confirms industry-specific comprehensive-income endpoints exist, but spot checks did not recover these current missing rows.
- Final pre-Git check regenerated `output/tw_valuation_gap_20260709_000616_no_cnyes.xlsx` and `_lite.xlsx` from current code with 25 listed + 25 OTC rows. Both files have valuation-source XML width 40 versus a longest source value length of 36, no blank source columns, zero row-level ROE notes, and zero blank cell nodes. Excel COM opened full in 163 ms and lite in 90 ms without repair and reported an effective source column width of 39.22 with the full TPEx endpoint visible. `py_compile`, `git diff --check`, and ignore checks passed.
- Widened only the user-facing `估值資料來源` column from 28 to 40 so the longest current value, `TPEx tpex_mainboard_peratio_analysis`, remains within the cell. Other source columns retain their existing widths to avoid unnecessarily widening the workbook.

## 2026-07-11

- Final pre-Git overall check generated `output/tw_valuation_gap_20260709_205053_no_cnyes.xlsx` and `_lite.xlsx` from the current code with 25 listed + 25 OTC rows. Validation found 47 complete and 3 partial fundamentals groups, no blank valuation/monthly-revenue/financial source cells, `TWSE`/`TPEx` exchange-source values split 25/25, zero row-level ROE notes, exchange-source XML width 12, and zero blank cell nodes. Excel COM opened full in 182 ms and lite in 91 ms without repair; `py_compile` and `git diff --check` passed. `.env`, `output/`, and `tmp/` remain ignored and no environment keys were changed in this work.
- Removed the repeated `ROE 暫未納入` sentence from every stock row's `fundamentals_note` at the user's request. The ROE limitation remains centralized in the workbook guide/dictionary and data-source documentation; row-level notes now contain only limitations that directly affect that stock row, such as the missing stable OTC gross-margin source.
- Compacted the user-facing `交易所資料來源` values from long endpoint lists to `TWSE` / `TPEx` and fixed the column width at 12. The exact close/profile endpoints remain in the adjacent `交易所資料說明` and workbook dictionary, preserving traceability while reducing stock-table clutter.
- Reworked the requested fundamentals semantics: each data group now separates source from note. The workbook orders `估值資料日 / 估值資料來源 / 估值資料備註`, `月營收期別 / 月營收資料來源 / 月營收資料備註`, and `財報期別 / 財報資料來源`. Renamed the final status to `基本面整體狀態`; it now reports whether the valuation, monthly-revenue, and financial-statement source rows are all present, partially present, all absent, or skipped, instead of treating any one populated field as overall success.
- Added `--stock-limit` for true small-output test runs. With both markets selected it takes an even split, so `--stock-limit 50` produces 25 listed and 25 OTC rows rather than retaining the full market and only limiting Cnyes requests.
- Completed the requested live 50-stock run with `--stock-limit 50`: `output/tw_valuation_gap_20260709_202419.xlsx` and `_lite.xlsx`, containing exactly 25 listed plus 25 OTC stocks. Cnyes was 50/50 `ok`; fundamentals aggregate status was 47 complete and 3 partial. XML validation confirmed the source columns immediately follow each date/period group, all three source columns are populated for all 50 rows, and there are zero blank cell nodes. Excel COM opened the lite workbook without repair and confirmed 51 used rows (header + 50) and the requested Z:AP header sequence.
- Clarified and surfaced the separate source boundaries requested by the user. Valuation date/PER/PBR/yield use TWSE `BWIBBU_ALL` or TPEx `tpex_mainboard_peratio_analysis`; revenue period and monthly/cumulative growth use TWSE `t187ap05_L` or TPEx `mopsfin_t187ap05_O`; financial period/EPS use TWSE `t187ap14_L` or TPEx `mopsfin_t187ap14_O`. Added `monthly_revenue_note` immediately after `revenue_period` in full/lite/stale outputs, plus guide/dictionary/README/docs explanations. Notes distinguish an absent official monthly-revenue row from specific official field gaps.
- Generated `output/tw_valuation_gap_20260709_200034_no_cnyes.xlsx` and `_lite.xlsx` for all 1,980 stocks. The final lite validation found 1,878 rows with complete monthly-revenue fields, 89 rows absent from the official monthly-revenue endpoint, and 13 rows with one or more official monthly-revenue growth fields missing; every revenue-related blank has a nonblank note. `valuation_metric_date` is now width 14 and renders on one line; `monthly_revenue_note` is width 28. XML inspection found zero blank cell nodes, and Excel-rendered Z:AJ visual QA passed.
- Renamed the user-facing `revenue_period` header from `營收期別` to `月營收期別` so it aligns with the adjacent `月營收資料備註`; synchronized the missing-field note, guide, dictionary, and README wording. Set `revenue_period` width to 14 so the new header renders on one line.
- Completed a fresh overall check after all width/wrap changes. Re-ran the real Cnyes watchlist flow with conservative pacing and produced `output/tw_valuation_gap_20260709_172026_watchlist.xlsx` plus `_lite.xlsx`: 3 rows, 3 undervalued, Cnyes 3/3 `ok`, and all representative Cnyes/fundamental fields nonblank. Full has 7 sheets and lite 6, both active on `低估清單`, with zero blank cell nodes, matching shared-string counts, no formulas/AutoFilter, and 6 valid Cnyes hyperlinks. Excel opened full in 133 ms without repair; visual QA passed for every sheet and key base/target/fundamental regions.
- The overall visual pass found the guide, dictionary, and fetch-status columns were still only width 10. Added targeted widths for `section`, `item`, `description`, `field`, and fetch-status metadata/source/URL/message columns without changing the stock-table widths. Final rendered guide/dictionary/status sheets are readable. Confirmed `.env` and `.env.example` each contain the same 14 keys, `py -3.11 -m py_compile src\tw_target_scan.py` and `git diff --check` pass, and `origin` remains `https://github.com/billshiou2/tw-stock-valuation-gap.git`.
- User requested the final overall check followed by Git sync. Pre-push checks confirmed cwd `C:\code\股價與目標價`, branch `main` tracking `origin/main`, matching project remote `https://github.com/billshiou2/tw-stock-valuation-gap.git`, ignored `.env`/`output`/`tmp`, and an intended commit set limited to `AGENT_PROGRESS.md`, `README.md`, `docs/data-sources.md`, and `src/tw_target_scan.py`.
- Widened `listing_date` and `close_date` to 14 so `上市櫃日期` and `最新收盤日期` render on one line while period-stamped fundamentals continue using two-line headers. Generated and visually verified `output/tw_valuation_gap_20260709_171604_watchlist_no_cnyes.xlsx`; A:H Excel rendering confirmed both date headers and date values fit without wrapping.
- Added explicit line breaks before period suffixes in generated data-table headers, so labels render as `殖利率` plus `(2026-07-09)` on the second line and `月營收(仟元)` plus `(2026-05)` on the second line. Increased the header row from 32 to 40 points. Kept `欄位說明` field names single-line by stripping display-only header newlines there. Final sample `output/tw_valuation_gap_20260709_171009_watchlist_no_cnyes.xlsx` has 14 period headers with explicit newlines, zero dictionary-field newlines, and passed Excel-rendered visual QA plus `py_compile`/`git diff --check`.
- Widened only the user-requested target/estimate Excel columns in `src/tw_target_scan.py`: target date and target high/low/mean/median to 14, estimate/up/down/currency to 12, target standard deviation to 16, and Cnyes page price to 14. Generated `output/tw_valuation_gap_20260709_170754_watchlist_no_cnyes.xlsx`; XML verification confirmed Y:AI widths exactly match the new values with zero blank cell nodes, and Excel-rendered visual QA confirmed all requested headers fit on one line. `py -3.11 -m py_compile src\tw_target_scan.py` and `git diff --check` passed (only expected LF-to-CRLF warnings).

## 2026-07-10

- Follow-up clarified that the complaint is post-open UI lag, not workbook-open time. Fresh XML inspection of the 20260709 Cnyes full/lite files found matching shared-string counts and references, no formulas, drawings, tables, conditional formatting, AutoFilter, or external workbook links, and Excel opened them without a repair path. COM post-open checks showed full recalculation at 0.5-5.5 ms and all-sheet switching at 41-162 ms; repeated background scroll timings were nearly identical before and after blank-cell optimization. Current evidence therefore does not indicate a malformed workbook; remaining UI lag should be isolated with Excel Safe Mode/add-in/display testing.
- Fixed the reported Excel lag/width/yield-blank issues in `src/tw_target_scan.py`. The XLSX writer now omits empty cell nodes, caps long/basic-fundamental column widths, wraps the 32-point header row, and adds `dividend_yield_note` immediately after dividend yield in full/lite/stale sheets. Blank yield notes distinguish `官方欄位空白（非爬取失敗）`, `未抓到官方估值資料`, and an unparseable source format.
- Generated and verified final no-Cnyes full-market samples `output/tw_valuation_gap_20260709_213843_no_cnyes.xlsx` and `_lite.xlsx` (1,980 rows). The lite `全部股票` sheet is `A1:AL1981`, has 52,739 cells with zero blank cell nodes, maximum width 24, wrapped headers, and all 246 blank-yield rows have a note: 233 official blank fields and 13 missing official valuation rows.
- Measured the final workbooks with Excel COM: Excel startup was 1,956 ms, lite workbook open 303 ms, and full workbook open 644 ms. Current connected COM add-ins were `LogiExcelAddIn` and `TCSCConv.SharedAddin.16`, so remaining startup lag is more consistent with Excel/add-in overhead than workbook formulas or structure. Visual QA of the fundamentals columns passed after widening fundamentals status to 20 while keeping the fundamental width cap compact.
- Updated `README.md` and `docs/data-sources.md` to explain the new yield-note semantics and compact wrapped Excel layout. `py -3.11 -m py_compile src\tw_target_scan.py` and `git diff --check` passed (only expected LF-to-CRLF warnings).

- Preparing Git sync for the latest fundamentals and workbook documentation changes. Pre-push checks: `py -3.11 -m py_compile src\tw_target_scan.py` passed, `git diff --check` only reported expected LF-to-CRLF warnings, `origin` is `https://github.com/billshiou2/tw-stock-valuation-gap.git`, and the intended commit set is limited to `AGENT_PROGRESS.md`, `README.md`, `docs/data-sources.md`, and `src/tw_target_scan.py`.
- Completed an overall check after the latest fundamentals/Cnyes workbook changes. Verified startup docs were reviewed, `py -3.11 -m py_compile src\tw_target_scan.py` passed, `.env` and `.env.example` have the same 14 non-secret keys, and `origin` points to `https://github.com/billshiou2/tw-stock-valuation-gap.git`. Current uncommitted changes remain in `AGENT_PROGRESS.md`, `README.md`, `docs/data-sources.md`, and `src/tw_target_scan.py`.
- Ran a real watchlist smoke test with conservative Cnyes pacing: `py -3.11 src\tw_target_scan.py --universe watchlist --cnyes-delay 1.0 --cnyes-retries 2 --cnyes-backoff 2.0 --cnyes-progress-every 1 --excel-output both`. Output files: `output/tw_valuation_gap_20260709_123429_watchlist.xlsx` and `_lite.xlsx`. Result: 3 rows, undervalued 3, overvalued 0, Cnyes 3/3 ok.
- Verified `output/tw_valuation_gap_20260709_123429_watchlist_lite.xlsx`: sheets are `使用說明`, `欄位說明`, `低估清單`, `高估清單`, `全部股票`, `抓取狀態`; active sheet is `低估清單`; no `autoFilter`; all three rows have nonblank Cnyes status/source/link plus PER/PBR/yield, revenue period, monthly/cumulative revenue growth, EPS, operating margin, net margin, and fundamentals status.

## 2026-07-07

- Generated a nonblank watchlist review sample with real Cnyes fetching after the user asked to try a small crawl: `output/tw_valuation_gap_20260707_234431_watchlist.xlsx` and `output/tw_valuation_gap_20260707_234431_watchlist_lite.xlsx`. Command used bat-equivalent conservative Cnyes pacing with `--universe watchlist --cnyes-delay 1.0 --cnyes-retries 2 --cnyes-backoff 2.0 --cnyes-progress-every 1 --excel-output both`. Result: 3 stock rows, undervalued 2, overvalued 0, Cnyes 3/3 `ok`. Verified the lite workbook opens on `低估清單`, includes `使用說明` and `欄位說明`, and the 2330/2317/2454 rows have nonblank target/fundamental fields including Cnyes status/source/link, PER/PBR/yield, monthly revenue YoY, cumulative revenue YoY, EPS, operating margin, and net margin.

- Added the recommended quick-scan fundamentals to `_lite`: PER, PBR, dividend yield, monthly revenue YoY, cumulative revenue YoY, EPS, operating margin, and net margin. Full workbook keeps the larger financial amount columns and source/note fields. Verified with `output/tw_valuation_gap_20260707_233026_watchlist_no_cnyes_lite.xlsx`; lite headers now include `累計營收年增率(2026-05)` between monthly revenue YoY and financial period.

- Added clearer gross-margin notes after user requested explicit notation. `fundamentals_note` now says listed-stock gross margin comes from TWSE `t187ap17_L`; OTC rows say OTC gross margin has no confirmed stable TPEx OpenAPI field and remains blank, so users should read operating margin and net margin instead. Excel guide now has a `毛利率註記` row explaining that operating margin is not substituted into gross margin. README and `docs/data-sources.md` were updated with the same warning. Verified with `output/tw_valuation_gap_20260707_232726_watchlist_no_cnyes.xlsx` that guide rows and listed row notes are present.

- Follow-up after user asked about Cnyes URL placement and gross margin. Moved `cnyes_url` / Excel header `鉅亨個股頁` to sit immediately after `cnyes_source` / `鉅亨資料來源` in full, lite, and stale/low-confidence stock lists. Verified with `output/tw_valuation_gap_20260707_231923_watchlist_no_cnyes.xlsx`: headers around Cnyes are `鉅亨抓取狀態`, `鉅亨請求次數`, `鉅亨資料來源`, `鉅亨個股頁`, then fundamentals. Confirmed listed-stock gross margin is populated from TWSE `t187ap17_L` (2330 = 66.25%, 2317 = 6.18%, 2454 = 46.30%). Confirmed OTC gross margin remains intentionally blank while operating margin is populated from TPEx income fields; verified `output/tw_valuation_gap_20260708_232009_no_cnyes.xlsx` has OTC `gross_blank=891` and `op_nonblank=890`.

- Integrated exchange fundamentals into the formal scanner output in `src/tw_target_scan.py`. New right-side Excel columns include valuation metric date, PER, PBR, dividend yield, revenue period, monthly revenue/growth, financial period, EPS, financial revenue, operating income, net income, gross margin, operating margin, net margin, fundamentals status/source/note. PER/PBR/yield come from TWSE `BWIBBU_ALL` or TPEx `tpex_mainboard_peratio_analysis`; monthly revenue comes from TWSE `t187ap05_L` or TPEx `mopsfin_t187ap05_O`; EPS/income comes from TWSE `t187ap14_L` or TPEx `mopsfin_t187ap14_O`; listed margins come from TWSE `t187ap17_L`; OTC gross margin remains blank due no confirmed stable TPEx OpenAPI margin endpoint. ROE remains deferred pending a stable public equity source.
- Added period-aware Chinese Excel headers for fundamentals, such as PER/PBR/yield using the valuation data date, monthly revenue using the revenue period, and EPS/income/margins using the financial period. Updated Excel guide/dictionary rows, README, and `docs/data-sources.md` to explain source boundaries, units, and limitations. Added CLI `--skip-fundamentals`; default `run_full_scan.bat` still runs the full suite.
- Verified `py -3.11 -m py_compile src\tw_target_scan.py`. Watchlist no-Cnyes smoke output succeeded: `output/tw_valuation_gap_20260703_004730_watchlist_no_cnyes.xlsx` and `_lite.xlsx`, with active sheet `低估清單`, no `autoFilter`, and fundamentals headers present.
- Ran an expanded 500-Cnyes test with bat-equivalent conservative pacing: `--cnyes-limit 500 --cnyes-delay 1.0 --cnyes-retries 2 --cnyes-backoff 2.0 --cnyes-progress-every 25 --excel-output both`. Output: `output/tw_valuation_gap_20260706_010318_cnyes_limit500.xlsx` and `_lite.xlsx`; rows 1,980; undervalued 15; overvalued 2; Cnyes status `ok=500, skipped_limit=1480`; no HTTP errors. Full workbook active sheet is `低估清單`, has no `autoFilter`, and includes 51 dictionary rows plus fundamentals headers.

- Produced a small fundamentals preview workbook for user review: `output/tw_financial_preview_20260707_003203.xlsx`. Sheets: `使用說明`, `基本面預覽`, `欄位說明`, `抓取狀態`; active sheet is `基本面預覽`. The preview uses watchlist stocks 2330, 2317, 2454 and includes period-stamped Chinese headers such as `本益比(2026-07-03)`, `月營收(2026-05)`, and `EPS(2026Q1)`, with thousands separators and percent text formatting. This was generated by temporary helper `tmp/make_financial_preview.py`; it is a preview artifact, not yet integrated into `src/tw_target_scan.py`.

- Re-tested the next-upgrade public data sources with network access. TWSE PER/PBR/yield `BWIBBU_ALL` returned HTTP 200, 1,078 rows, and watchlist matches for 2317, 2330, 2454 with fields `Date`, `Code`, `PEratio`, `DividendYield`, `PBratio`. TPEx PER/PBR/yield returned HTTP 200 and 889 rows. TWSE monthly revenue `t187ap05_L` returned HTTP 200, 1,082 rows, and watchlist matches for 2317, 2330, 2454 with monthly revenue, MoM %, YoY %, cumulative revenue, and cumulative YoY fields. TPEx monthly revenue returned HTTP 200 and 890 rows. Local sandbox still blocks direct sockets without escalation, but the public endpoints are reachable when network execution is allowed.
- Continued financial-statement source testing. Confirmed TWSE `https://openapi.twse.com.tw/v1/opendata/t187ap14_L` returns 1,078 rows with `年度`, `季別`, `公司代號`, `基本每股盈餘(元)`, `營業收入`, `營業利益`, `營業外收入及支出`, and `稅後淨利`; watchlist rows for 2317, 2330, and 2454 were found. Confirmed TPEx `https://www.tpex.org.tw/openapi/v1/mopsfin_t187ap14_O` returns 891 rows with the same EPS/income shape for OTC companies. Confirmed TWSE `https://openapi.twse.com.tw/v1/opendata/t187ap17_L` returns 1,048 rows with `毛利率(%)`, `營業利益率(%)`, `稅前純益率(%)`, and `稅後純益率(%)`; watchlist rows for 2317, 2330, and 2454 were found. TPEx Swagger only showed `mopsfin_t187ap14_O` for EPS and did not reveal an equivalent OTC margin-ratio endpoint. MOPS HTML endpoints `ajax_t163sb04/05/06` returned a security-block page, so they are not a good automation source for ROE/equity at this stage.

## 2026-07-06

- User asked whether the current valuation-gap analysis has reference value. Positioning recorded: the current workbook is useful as a first-pass full-market screener for candidates that may deserve manual review, because exchange close prices are joined with Cnyes `targetValuation` consensus target prices and rows are marked for stale valuation date, low estimate count, or missing target data. It should not be treated as a direct buy/sell conclusion.
- Suggested next data-quality upgrade path: add fundamentals such as recent EPS, ROE, monthly revenue growth, PER, PBR, and dividend yield so valuation gaps can be checked against business fundamentals. Candidate sources to evaluate first are official TWSE/TPEx/MOPS OpenAPI-style JSON endpoints where available, with third-party APIs only as convenience fallbacks.
- Live source probe for the next upgrade: TWSE `https://openapi.twse.com.tw/v1/exchangeReport/BWIBBU_ALL` returned 1,078 rows with `Date`, `Code`, `PEratio`, `DividendYield`, and `PBratio`; TPEx `https://www.tpex.org.tw/openapi/v1/tpex_mainboard_peratio_analysis` returned 889 rows with `Date`, `SecuritiesCompanyCode`, `PriceEarningRatio`, `YieldRatio`, and `PriceBookRatio`. TWSE monthly revenue `https://openapi.twse.com.tw/v1/opendata/t187ap05_L` returned 1,082 rows with `資料年月`, monthly revenue, MoM %, YoY %, cumulative revenue, and cumulative YoY %; TPEx `https://www.tpex.org.tw/openapi/v1/mopsfin_t187ap05_O` returned 890 rows with the same shape. Financial statement candidates `t187ap06_L` / `mopsfin_t187ap06_O` were not confirmed: TWSE timed out once and TPEx returned HTML, so EPS/ROE still require a separate endpoint check or calculation path.

## 2026-07-04

- Completed an overall check after adding per-stock Cnyes source detection, clickable Cnyes hyperlinks, and `欄位說明` in lite. Verified `py -3.11 -m py_compile src\tw_target_scan.py`, `.env` and `.env.example` both have the same 14 keys, remote is `https://github.com/billshiou2/tw-stock-valuation-gap.git`, and generated current watchlist outputs `output/tw_valuation_gap_20260703_230232_watchlist.xlsx` / `_lite.xlsx` with Cnyes 3/3 ok. Verified full sheets `使用說明`, `欄位說明`, `低估清單`, `高估清單`, `全部股票`, `過舊低信心`, `抓取狀態`; lite sheets `使用說明`, `欄位說明`, `低估清單`, `高估清單`, `全部股票`, `抓取狀態`; both active on `低估清單`, no `autoFilter`, Cnyes external hyperlinks present, and `鉅亨/FactSet共識` appears in source values.
- Added `欄位說明` to `_lite` workbooks as the second sheet, next to `使用說明`; active sheet remains `低估清單`. Verified with offline `tmp/lite_dictionary_check.xlsx`: sheets are `使用說明`, `欄位說明`, `低估清單`, `高估清單`, `全部股票`, `抓取狀態`.
- Generated a small watchlist output using the same conservative Cnyes crawl settings as `run_full_scan.bat` plus `--universe watchlist --excel-output both`. Outputs: `output/tw_valuation_gap_20260703_225452_watchlist.xlsx` and `output/tw_valuation_gap_20260703_225452_watchlist_lite.xlsx`. Rows: 3, undervalued: 2, overvalued: 0, Cnyes 3/3 ok. Verified workbook tabs, active `低估清單`, no `autoFilter`, `鉅亨資料來源` header present before final `鉅亨個股頁`, and external Cnyes hyperlinks present in the stock sheets.
- Changed `鉅亨資料來源` to be detected per stock page instead of hard-coded for all rows. The Cnyes parser now reads `targetValuation` without requiring a following `factSetEstimate` key, then labels rows as `鉅亨/FactSet共識` only when that page contains `FactSet` or `factSetEstimate`; otherwise it uses `鉅亨共識(來源未明確標示)`, with separate labels for no target and not fetched.
- Corrected the `鉅亨資料來源` wording after user observed that Cnyes forecast/estimate source can be FactSet. The workbook value is `鉅亨/FactSet共識` only when the stock page contains a FactSet marker; docs clarify that Refinitiv is a broader page-footer quote/market-data attribution.
- Updated the custom XLSX writer so `鉅亨個股頁` (`cnyes_url`) cells are real Excel external hyperlinks with worksheet `.rels` entries and blue underlined hyperlink styling; README now notes that the source page can be opened directly from the workbook.
- Added a short Excel column `鉅亨資料來源` (`cnyes_source`) before `鉅亨個股頁`; the value is now detected per page instead of hard-coded. Updated guide, dictionary, README, and docs to explain that target valuation comes from Cnyes embedded `targetValuation`, with FactSet shown only when the page has that marker; this is consensus data, not broker-by-broker research and does not identify individual institutions.
- User explicitly requested committing one latest output sample so users can preview the result. Chose the latest full-market workbook `output/tw_valuation_gap_20260703_172748.xlsx` as the single tracked sample despite the default `output/` ignore rule; day-to-day generated outputs remain ignored.
- Completed a full end-to-end production run through `cmd /c run_full_scan.bat --no-pause`. Runtime was about 53.8 minutes. Outputs: `output/tw_valuation_gap_20260703_172748.xlsx` (553,861 bytes) and `output/tw_valuation_gap_20260703_172748_lite.xlsx` (295,539 bytes). Rows: 1,980 total, 79 undervalued, 35 overvalued. Cnyes result: `ok=1977`, `no_target_valuation=3`, no HTTP errors. Verified full workbook sheets `使用說明`, `欄位說明`, `低估清單`, `高估清單`, `全部股票`, `過舊低信心`, `抓取狀態`; lite sheets `使用說明`, `低估清單`, `高估清單`, `全部股票`, `抓取狀態`; both open on `低估清單`, have `sharedStrings`, no `autoFilter`, Cnyes URLs, and Chinese success status. Fetch status dates: TWSE/TPEx profiles 2026-07-04, daily close 2026-07-03, report date 2026-07-03.
- Completed an overall check after sheet-order and guide cleanup. Verified `py -3.11 -m py_compile src\tw_target_scan.py`, `.env` and `.env.example` both have 14 matching keys, Chinese display mappings work, no local Git changes before the check, and generated fresh watchlist Cnyes samples `output/tw_valuation_gap_20260703_162844_watchlist.xlsx` / `_lite.xlsx` with Cnyes 3/3 ok. Full sample sheets are `使用說明`, `欄位說明`, `低估清單`, `高估清單`, `全部股票`, `過舊低信心`, `抓取狀態`; full and lite both open on `低估清單`, have no `autoFilter`, include Cnyes URLs, and show `成功讀取鉅亨資料`.
- Moved full workbook `欄位說明` next to `使用說明`, making the first sheets `使用說明`, `欄位說明`, `低估清單`. Updated `write_xlsx()` to select `低估清單` by sheet name instead of a fixed sheet index; verified sample `output/tw_valuation_gap_20260703_162605_watchlist_no_cnyes.xlsx` has activeSheet `低估清單`.
- Generated a fresh watchlist sample for review after guide/dictionary cleanup: `output/tw_valuation_gap_20260703_161930_watchlist.xlsx` and `output/tw_valuation_gap_20260703_161930_watchlist_lite.xlsx`. Watchlist rows were 2330, 2317, 2454; Cnyes fetch was 3/3 ok. Verified sheets include `使用說明`, `欄位說明` in full output, the guide pointer `逐欄查詢`, dictionary label `平均目標價 (target_mean)`, and `鉅亨個股頁`.
- Cleaned up overlap between Excel `使用說明` and `欄位說明`: removed repeated per-column `中文表頭` explanations from the guide, added a concise pointer to the dictionary sheet, and changed dictionary `欄位` values to show the Chinese header with the internal key in parentheses, such as `平均目標價 (target_mean)`. README wording now describes user-facing Chinese statuses instead of raw internal codes.
- Clarified average target price source in Excel `使用說明` and README: `平均目標價` comes from Cnyes embedded `targetValuation.feMean`, not from averaging high/low targets; manual checks should use the Cnyes stock page `預估` tab, but the UI may not literally show the phrase `平均目標價`.
- Changed generated Excel filenames to include both latest close date and run time (`tw_valuation_gap_YYYYMMDD_HHMMSS...xlsx`) so rerunning on the same trading day does not overwrite prior files. README examples were updated to show the timestamped naming.
- Generated a small watchlist sample without full-market crawling after adding the Cnyes URL column: `output/tw_valuation_gap_20260703_watchlist.xlsx` and `output/tw_valuation_gap_20260703_watchlist_lite.xlsx`. Watchlist rows were 2330, 2317, 2454; Cnyes fetch was 3/3 ok. Verified `全部股票` ends with `鉅亨個股頁` and URLs like `https://www.cnyes.com/twstock/2330`.
- Added `鉅亨個股頁` (`cnyes_url`) back as the last column of the generated full, lite, and stale/low-confidence stock lists. The value is generated from the stock id as `https://www.cnyes.com/twstock/{stock_id}` so users can manually open the source page and check embedded `targetValuation`; guide/dictionary/README wording was updated accordingly.
- Optimized Excel open performance after workbook-lag feedback: generated workbooks now use `xl/sharedStrings.xml` instead of repeated `inlineStr` cells, and the main valuation sheets no longer output the verbose per-row `exchange_note`, `cnyes_url`, or `cnyes_error` columns. Refreshed artifact without re-crawling: `output/tw_valuation_gap_20260703_fast.xlsx`; compared with `readable_complete`, sheet XML dropped from 5,215,610 to 3,523,910 bytes, total cells dropped from 112,277 to 104,228, no `autoFilter`, active tab remains `低估清單`.
- Investigated remaining Excel open hangs: data volume was not the main issue; `openpyxl` and Excel COM could load the data quickly, but the custom XLSX package was too minimal (`fills count=1`, no docProps, incorrect sharedStrings count). Updated the writer to emit standard core/app properties, `fills count=2`, and accurate shared string counts. Refreshed artifact: `output/tw_valuation_gap_20260703_excel_compatible.xlsx`; Excel COM open timing was about 0.136s vs 0.347s for the earlier fast file.
- Added plain-language TWSE/TPEx definitions to the Excel `使用說明` sheet: TWSE = Taiwan Stock Exchange / listed stocks, TPEx = Taipei Exchange / OTC stocks. Refreshed artifact without re-crawling: `output/tw_valuation_gap_20260703_guide_twse_tpex.xlsx`.
- Added `.env`-driven settings for valuation thresholds and output mode. Defaults are documented in `.env.example`; CLI args still override `.env`. `run_full_scan.bat` now passes `--excel-output both`, so one scan writes both the full workbook and a `_lite` workbook. Excel `使用說明` now explains why stale/low-confidence/missing-target rows are excluded from the main undervalued/overvalued sheets.
- Localized Excel display values for `valuation_signal`, `confidence_note`, and `cnyes_status` while keeping internal codes unchanged. Examples: `stale_618_days` displays as `評價過舊 618 天`, `missing_cnyes_target` displays as `缺少鉅亨目標價`, and `http_error` displays as `抓取失敗`.
- Restored exchange trading columns in the readable artifact by merging close/trade fields back from the no-Cnyes exchange-only workbook; `交易所資料來源` and `交易所資料說明` are also auto-filled when missing before workbook output. Refreshed artifact: `output/tw_valuation_gap_20260703_readable_complete.xlsx`; only 3 stocks remain blank for close/trade fields because the source close data has no row/value for them: 1591, 4804, 1589.
- Suppressed Excel `numberStoredAsText` warnings for `股票代號` and `產業別` columns while keeping them stored as text to preserve stock-code and industry-code semantics. Refreshed artifact: `output/tw_valuation_gap_20260703_readable_no_warnings.xlsx`.
- Removed the extra Excel `交易所資料` and `產業別說明` worksheets from generated workbooks after user feedback. Added `industry_name` / `產業名稱` immediately next to `industry` / `產業別` in the main lists, with code-to-name mapping applied in-row. Refreshed artifact: `output/tw_valuation_gap_20260703_readable_industryname.xlsx` with 1980 rows, 78 undervalued, 35 overvalued, active tab on `低估清單`.
- Added an Excel `產業別說明` sheet with TWSE/TPEx industry-code explanations, updated the guide/dictionary wording for `產業別`, fixed the latest readable workbook by restoring `最新收盤日期` from source status dates, and slimmed `過舊低信心` to 15 key columns to reduce workbook load. Refreshed artifact: `output/tw_valuation_gap_20260703_readable_fixed.xlsx` with 1980 rows, 78 undervalued, 35 overvalued.
- Added detailed crawl-method notes to the Excel `使用說明` sheet: TWSE/TPEx are OpenAPI JSON downloads, Cnyes is HTML page fetching with embedded `targetValuation` JSON parsing via Python `urllib`, and the guide now documents request pacing, retries/backoff, `run_full_scan.bat` conservative settings, `--cnyes-limit`, `--skip-cnyes`, and `skipped_error_threshold`. Refreshed artifact: `output/tw_valuation_gap_20260703_readable_crawlinfo.xlsx`.
- Clarified exchange trading units in Excel output: `成交股數(股)`, `成交金額(元)`, and `成交筆數(筆)`. The guide and data dictionary now state that TWSE `TradeVolume` and TPEx `TradingShares` are shares, not lots, and that TWSE `TradeValue` / TPEx `TransactionAmount` are NTD. Refreshed artifact: `output/tw_valuation_gap_20260703_readable_units.xlsx`.
- Renamed the Excel display header for `close_date` from `收盤日期` to `最新收盤日期`, and updated guide/dictionary wording to match. Refreshed artifact: `output/tw_valuation_gap_20260703_readable_latestclose.xlsx`, verified no `autoFilter`, 1980 all-stock rows, 78 undervalued rows, and 35 overvalued rows.
- Changed Excel display for the `市場` column from internal codes (`tse`/`otc`) to user-facing values (`上市`/`上櫃`) while keeping internal code handling for joins. Refreshed artifact: `output/tw_valuation_gap_20260703_readable.xlsx`, verified no `autoFilter`, 1980 all-stock rows, 78 undervalued rows, and 35 overvalued rows.
- Removed default Excel `autoFilter` output after the user noted the workbook does not need to open in filter mode. A refreshed artifact was written to `output/tw_valuation_gap_20260703_sources_nofilter.xlsx`; verification found no `autoFilter` XML parts and retained 1980 all-stock rows, 78 undervalued rows, and 35 overvalued rows.
- Added a new Excel worksheet `交易所資料` to separate TWSE/TPEx-only data from the joined valuation view. New generated workbooks now open on `低估清單` as sheet 3. Because the prior full workbook was locked by Excel, a refreshed full artifact was written to `output/tw_valuation_gap_20260703_sources.xlsx` with 1980 all-stock rows, 78 undervalued rows, and 35 overvalued rows.
- Updated the Excel `使用說明` guide and `欄位說明` data dictionary to match the localized worksheet names and Chinese headers. Existing full output `output/tw_valuation_gap_20260703.xlsx` was refreshed in place without re-crawling, and the watchlist no-Cnyes smoke test was regenerated successfully.
- Implemented the planned Taiwan valuation-gap Excel workflow in `src/tw_target_scan.py`.
- Main source is now daily close data, not TWSE MIS intraday quotes: TWSE `STOCK_DAY_ALL` for listed stocks and TPEx `tpex_mainboard_daily_close_quotes` for OTC stocks.
- Company universe is joined from TWSE `t187ap03_L` and TPEx `mopsfin_t187ap03_O` so `All_Stocks` is one row per listed/OTC company.
- Cnyes target valuation is fetched from `https://www.cnyes.com/twstock/{stock_id}` embedded `targetValuation`; missing target data keeps the row and sets `cnyes_status`.
- Output is now `.xlsx` only, written under `output/` as `tw_valuation_gap_YYYYMMDD.xlsx`, with sheets `Undervalued`, `Overvalued`, `All_Stocks`, `Stale_or_Low_Confidence`, `Fetch_Status`, and `Data_Dictionary`.
- Added a `Guide` sheet explaining column groups, field names, and the user's question that valuation-gap columns compare exchange close prices against Cnyes consensus target prices.
- If the target `.xlsx` is locked by Excel, the script now writes a timestamp-suffixed filename instead of failing.
- Non-default test modes add filename suffixes (`_watchlist`, `_no_cnyes`) so validation runs do not overwrite the default full-market report.
- README and `docs/data-sources.md` were updated for the new daily-close + Cnyes consensus workflow.
- Added safer Cnyes crawling controls: browser-like headers, default `--cnyes-delay 0.5`, `--cnyes-retries`, exponential backoff, progress logging, `--cnyes-limit`, and a high HTTP-error early stop (`skipped_error_threshold`).
- Added short retry handling for TWSE/TPEx JSON downloads after a TPEx close-data `IncompleteRead` happened during testing.
- Verified `py -3.11 -m py_compile src\tw_target_scan.py`.
- Verified watchlist Cnyes run with 3/3 Cnyes `ok`.
- Verified all-market sample with `--cnyes-limit 20 --cnyes-delay 0.5 --cnyes-retries 1`: output `output/tw_valuation_gap_20260703_cnyes_limit20.xlsx`, 1980 stock rows, Cnyes 20/20 `ok`, remaining rows marked `skipped_limit`.
- Added `run_full_scan.bat` as the main Windows entrypoint. It runs the full scanner with conservative Cnyes settings: `--cnyes-delay 1.0`, `--cnyes-retries 2`, `--cnyes-backoff 2.0`, progress every 25 stocks, and high-error early stop protection.
- README now documents the `.bat` workflow, `--no-pause`, and the small `--cnyes-limit 20` stability test.
- Completed a full all-market run through `cmd /c run_full_scan.bat --no-pause`. Output: `output/tw_valuation_gap_20260703.xlsx`, 1980 rows, 78 undervalued, 35 overvalued. Cnyes fetch result: `ok=1968`, `no_target_valuation=12`, no HTTP errors. Runtime was about 55 minutes.
- Pre-push safety check found existing `origin` still points to `https://github.com/billshiou2/00_project-template.git`; user requested target repo `https://github.com/billshiou2/tw-stock-valuation-gap.git`. Remote must be changed only after explicit confirmation because it currently points to a different project.
- Updated Excel output formatting after user asked for direct Chinese headers and thousands separators. `src/tw_target_scan.py` now keeps internal English keys but writes Chinese headers to `.xlsx`; prices/targets use `#,##0.00`, integer quantities/counts use `#,##0`, and percentage fields remain `0.00%`.
- Rewrote the existing full output workbook `output/tw_valuation_gap_20260703.xlsx` in place without re-crawling data so it now has Chinese headers and thousands number formats.
- Updated Excel header styling after user asked for centered headers and no dotted-looking background fill. Header cells now use bold centered text with no fill; existing full workbook styles were refreshed in place without re-crawling.
- Localized Excel worksheet names after user asked for Chinese sheet tabs. New generated workbooks use `低估清單`, `高估清單`, `全部股票`, `過舊低信心`, `抓取狀態`, `使用說明`, and `欄位說明`; existing full workbook sheet names were updated in place without re-crawling.
- Reordered Excel tabs after user asked to put `使用說明` first while opening by default on `低估清單`. New generated workbooks place `使用說明` first, `低估清單` second, and set `activeTab=1`; existing full workbook was updated in place without re-crawling.

## 2026-06-12

- 使用者想建立台股「目前價格 vs 研究機構/券商目標價」掃描工具，偏好大間機構如元大、統一，並詢問是否有公開資源。
- 已建立第一版 Python 標準函式庫工具 `src/tw_target_scan.py`，用 TWSE 公開 MIS 行情端點抓上市/上櫃即時或最近行情。
- 已新增 `config/watchlist.csv` 作為掃描股票清單，`config/target_prices.csv` 作為可追溯目標價匯入表。
- 已新增 `docs/data-sources.md` 說明：台股行情可用公開交易所端點；券商目標價目前沒有集中免費官方 API，先以 CSV 保存來源、機構、日期與 URL，後續可再接公開新聞或付費資料源。
- README 已加入執行方式：`py -3.11 src/tw_target_scan.py`，輸出在 `output/`；本機 `py -3` 會解析到 Windows Store alias 而失敗。
- 已用 `py -3.11 src\tw_target_scan.py` 實際連 TWSE 公開行情端點驗證，成功產生 `output/tw_target_scan_summary.csv`、`output/tw_target_scan_detail.csv`、`output/tw_target_scan_report.md`。

## 目前目標

- 建立可直接使用的乾淨專案起始結構，並讓後續 agent 能接續目前進度。

## 目前進度

- 已保留 `AGENTS.md` 原本的套件版本安全規則。
- 已追加專案結構、機密管理與 agent 進度記錄規則。
- 已建立 `README.md`、`.gitignore`、`.env.example` 與基礎資料夾。
- 已新增 `.gitkeep` 以保留基礎空資料夾。
- 已新增 `docs/git-workflow.md`，記錄人工建立 GitHub repo 後的 Git 上傳流程與上傳前檢查。
- 已在 `README.md` 加入 Git 上傳流程入口。
- 使用者決定要將此專案上傳到 Git；已完成本機 Git 初始化、remote 設定與 GitHub push。
- 已補強 Git 上傳前確認規則：push 前必須核對目前資料夾、專案說明、branch / upstream、remote URL 或準備設定的 repo URL 與使用者指定目標是否能合理對應；若看起來像不同專案或與指定目標不符，必須停止並再次確認。

## 已決定事項

- 原本的套件版本安全規則不得刪除、修改或弱化。
- 不建立 `docs/DECISIONS.md`，交接重點集中記錄在 `AGENT_PROGRESS.md`。
- 不預設 Node、Python、React 或其他特定框架。
- 不使用「範本」、`template`、「請替換」等字眼描述本專案。
- `.env` 放在專案根目錄但不得提交，`.env.example` 可以提交。
- 新增、刪除或修改 `.env` 的環境變數時，必須同步更新 `.env.example`。
- `.env.example` 是環境變數名稱、格式與用途說明來源。
- `.env` 與 `.env.example` 的非敏感預設值應盡量保持一致；敏感或本機專用值可以不同。
- 任務收尾前必須檢查 `AGENT_PROGRESS.md`、環境變數同步、`README.md` 更新需求與測試執行情況。
- `config/` 放非機密、可提交的設定。
- `docs/` 放文件、需求、規格與說明。
- `output/` 放產生輸出，預設不提交。
- `src/` 放正式原始碼。
- `tmp/` 放暫存檔，預設不提交。
- 使用 `.gitkeep` 保留 `config/`、`docs/`、`src/`、`output/`、`tmp/` 的資料夾結構。
- GitHub repo 建議先由使用者在網頁人工建立，確認名稱、private/public 與權限後，再用 Git 指令上傳。
- GitHub CLI 不列入預設流程，只作為未來需要自動化時的可選方式。
- 執行 `git push` 或設定 / 修改 remote 前，必須確認本機專案名稱 / 路徑、README 或專案說明、branch、upstream 與 remote repo 名稱或準備設定的 repo URL 能合理對應到同一個專案；若看起來像不同專案或與使用者明確指定目標不符，必須先通知使用者並等待再次確認。
- 若既有 remote 看起來指向不同專案，不得自行覆蓋或 push，必須先回報差異。

## 歷程重點

- 討論後決定採用精簡結構，避免一開始加入過多管理文件。
- 討論後決定使用 `AGENT_PROGRESS.md` 記錄目前進度、重點討論、待確認事項與下一步。
- 討論後決定資料夾說明寫在 `AGENTS.md` 與 `README.md`，不在每個資料夾各放說明檔。
- 討論後決定修改 `.env` 時必須同步更新 `.env.example`。
- 參考 `poly-rewards4` 規則後，決定補強 `.env.example` 說明來源、非敏感預設值一致與任務收尾檢查規則。
- 討論後決定加入 `.gitkeep`，避免空資料夾在 Git 中消失。
- 討論後決定補上 Git 上傳流程，採用人工建立 GitHub repo 加本機 Git 指令 push 的保守流程。
- 討論後決定開始將目前專案上傳到 Git；已完成本機 Git 初始化、安全檢查與第一個 commit，並設定 remote 為 `https://github.com/billshiou2/00_project-template.git`。
- 嘗試 push 時，網路沙盒先阻擋連線；使用者批准後，已成功推送到 GitHub。
- 因 Git 偵測 repo ownership 與目前 Windows 使用者不同，已依 Git 提示將 `C:/# code/# project-template3` 加入全域 `safe.directory`。
- 使用者提醒曾發生 Git 上傳錯 repo 的風險，因此已將 push 前專案名稱 / remote / branch 核對流程寫入 `AGENTS.md`、`docs/git-workflow.md` 與 `README.md`；後續又確認不要求名稱逐字相同，只要求能合理對應。
- 使用者要求 push 本次 Git 上傳前確認規則更新；push 前已核對目前路徑、remote、branch 與變更檔案能合理對應到本專案。

## 待確認事項

- 無。

## 下一步

- 依實際專案需求新增原始碼、設定或文件。
- 若新增套件，必須遵守 `AGENTS.md` 的套件版本安全規則。
- 新專案第一次上傳 GitHub 時，參考 `docs/git-workflow.md`。
- 目前 `main` 已設定追蹤 `origin/main`，後續一般更新可使用 `git push`。
